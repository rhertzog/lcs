/var/log/rubycas-server/*.log {
        weekly
        missingok
        rotate 52
        compress
        notifempty
        postrotate
           invoke-rc.d rubycas-lcs restart > /dev/null
		endscript
}