Use cdt_plug;
CREATE TABLE `postit_eleve` (`id` MEDIUMINT NOT NULL AUTO_INCREMENT PRIMARY KEY ,
`login` VARCHAR( 30 ) NULL DEFAULT NULL ,`texte` BLOB NULL DEFAULT NULL ) ENGINE = MYISAM ;
ALTER TABLE `onglets` ADD `edt` VARCHAR( 50 ) NULL DEFAULT NULL ;
ALTER TABLE `onglets` ADD `mod_cours` BLOB NOT NULL ,ADD `mod_afaire` BLOB NOT NULL ;