Use cdt_plug;
ALTER TABLE  `onglets` ADD  `cologin` VARCHAR( 30 ) CHARACTER SET latin1 COLLATE latin1_swedish_ci NULL AFTER  `login` ;
ALTER TABLE `onglets` CHANGE `prefix` `prefix` VARCHAR(15) CHARACTER SET latin1 COLLATE latin1_swedish_ci NULL DEFAULT NULL ;
