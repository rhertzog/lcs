/var/log/openvpn/*.log {
        weekly
        missingok
        rotate 52
        postrotate
        	invoke-rc.d --quiet openvpn reload > /dev/null
        	chmod +r /var/log/openvpn/status.log
       	endscript
        compress   			
        notifempty
}
