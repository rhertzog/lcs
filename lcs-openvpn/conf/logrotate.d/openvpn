/var/log/openvpn/*.log {
        weekly
        missingok
        rotate 52
        postrotate
        		invoke-rc.d --quiet openvpn reload > /dev/null
       	endscript
        compress   			
        notifempty
}
