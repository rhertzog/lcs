# The LCS adds scripts that do not have manual pages. Since none of those
# programs are for general use but are only used by other LCS components,
# it's not a big deal. Ignore this warning.
binary-without-manpage
