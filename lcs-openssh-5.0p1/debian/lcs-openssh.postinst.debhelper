# Automatically added by dh_installinit
if [ -x "/etc/init.d/lcs-openssh" ]; then
	update-rc.d lcs-openssh defaults >/dev/null
	if [ -x "`which invoke-rc.d 2>/dev/null`" ]; then
		invoke-rc.d lcs-openssh start || exit $?
	else
		/etc/init.d/lcs-openssh start || exit $?
	fi
fi
# End automatically added section
